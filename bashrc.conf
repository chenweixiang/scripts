################################################################################
# Common commands
################################################################################

alias ll="ls --color=auto -lh"
alias lla="ls --color=auto -lha"
alias dusum="du -sh"


################################################################################
# set language to English
################################################################################

export LC_ADDRESS="en_US.UTF-8"
export LC_IDENTIFICATION="en_US.UTF-8"
export LC_MEASUREMENT="en_US.UTF-8"
export LC_MONETARY="en_US.UTF-8"
export LC_NAME="en_US.UTF-8"
export LC_NUMERIC="en_US.UTF-8"
export LC_PAPER="en_US.UTF-8"
export LC_TELEPHONE="en_US.UTF-8"
export LC_TIME="en_US.UTF-8"


################################################################################
# set mailbox for mutt
################################################################################

MAIL=/home/chenwx/mail/inbox && export MAIL

# fix "M-b~T~BM-b~T~TM-b~T~@>" problem when mutt in thread mode
export LANG=en_US.ISO-8859-1

# fix warning from gedit: Gtk-WARNING **: Locale not supported by C library.
# fix "M-b~T~BM-b~T~TM-b~T~@>" problem when mutt in thread mode
export LC_ALL=C


################################################################################
# alias to ~/linux/script/checkpatch.pl
################################################################################

alias checkpatch="~/linux/scripts/checkpatch.pl -terse -file"


################################################################################
# commands for programming
################################################################################

# find specified file in current directory
alias findf="find . -noignore_readdir_race -nowarn -name"

# find installed kernel, e.g.: findkernel "*3.15*"
alias findkernel="find /boot /var -ignore_readdir_race -nowarn -name"

# find specified keyword in header files (.h)
alias findh="find . -noignore_readdir_race -nowarn -name '*.h' | xargs grep --color -n -s"

# find specified keyword in source files (.c)
alias findc="find . -noignore_readdir_race -nowarn -name '*.c' | xargs grep --color -n -s"

# find specified keyword in header and source files (.h, .c)
alias findahc="find . -noignore_readdir_race -nowarn -name '*.[hc]' | xargs grep --color -n -s"

# find specified keyword in all kind of files
alias findaf="find . -noignore_readdir_race -nowarn -type f | xargs grep --color -n -s"

# remove tail spaces, used when "git commit" failed caused by pre-commit script
# usage: rmtailspace lib.c > lib.c.notailspace
alias rmtailspace="sed -e \"s/[ \t]*$//g\""


################################################################################
# alias for building Linux Kernel
################################################################################

# cross compile Linux Kernel for ARM
# usage 1: config linux kernel - makearm *config, makearm acs5k_defconfig, ...
# usage 2: build linux kernel - makearm
alias makearm="make ARCH=arm CROSS_COMPILE=arm-linux-gnueabi-"


################################################################################
# alias for git commands (already moved to git alias, see 'git config --list')
################################################################################

#source ~/scripts/git-config.conf


################################################################################
# git commands auto-completion
################################################################################

#source ~/scripts/git-completion.conf


################################################################################
# build git commands
################################################################################

# 1) clean git
alias cleangit="make distclean"

# 2) build git
alias buildgit="make prefix=/usr all doc info"

# 3) install built git
alias installgit="sudo make prefix=/usr install install-doc install-html install-info"


################################################################################
# ClearCase commands
################################################################################

alias vtree="cleartool lsvtree -g"
alias ctco="cleartool co -nc"
alias lsco="cleartool lsco -a -cview -short"
alias cols="cleartool ls -l \`cleartool lsco -a -cview -short\`"
alias ctls="cleartool ls -l"
alias unco="cleartool unco -keep"

alias ctdiff="cleartool diff -g"
alias diffpre="cleartool diff -g -pre"

alias lsall="cleartool lspriv -other"
alias rmall="rm -rf \`cleartool lspriv -other\`"


